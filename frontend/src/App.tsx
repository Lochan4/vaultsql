// Placeholder — filled in Stage 4 (Layout Shell)
export default function App() {
  return (
    <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100vh' }}>
      <p style={{ fontFamily: 'var(--font-ui)', color: 'var(--fg)', fontSize: 'var(--text-md)' }}>
        VaultSQL — scaffolding ready
      </p>
    </div>
  )
}
